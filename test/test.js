(function () {
  var nx = require('next-js-core2');
  var NxWeappClipboard = require('../src/next-weapp-clipboard');

  describe('NxWeappClipboard.methods', function () {
    test('init', function () {
      var data = {
        key: 1,
        value: 2
      };
      // console.log(rs1, rs2);

      // expect(rs1).toBe(true);

    });
  });
}());
